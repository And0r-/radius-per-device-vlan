#!/bin/bash
# Seed script placeholder.
# All devices and users are added via manage.sh after deployment.
# This file exists so the docker-entrypoint-initdb.d pattern is documented.
set -e
echo "==> Schema ready. Add users and devices via manage.sh."
