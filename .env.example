# FreeRADIUS Stack — copy to .env and fill in your values
# Generate secrets: openssl rand -base64 18 | tr -d '/+=' | head -c 24

# PostgreSQL
POSTGRES_USER=radius
POSTGRES_PASSWORD=CHANGE_ME_generate_random
POSTGRES_DB=radius

# RADIUS shared secret (between UniFi AP and RADIUS server)
RADIUS_SECRET=CHANGE_ME_generate_random

# Enterprise user passwords (WPA2/WPA3-Enterprise)
ANDI_DESKTOP_PASS=CHANGE_ME
ANDI_MOBILE_PASS=CHANGE_ME
WORK_LAPTOP_PASS=CHANGE_ME

# Enterprise device MACs (lowercase, no separators, e.g. aabbccddeeff)
MAC_ANDI_DESKTOP=
MAC_ANDI_MOBILE=
MAC_WORK_LAPTOP=
